# Ноутбуки

- `01_eda_and_experiments.ipynb` - минимальный EDA и проверка эксперимента через модули из `src/`.

Основной код проекта вынесен в `src/`, поэтому ноутбук нужен для демонстрации анализа, а не для единственного способа запуска решения.

