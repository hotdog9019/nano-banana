# HW12

Требования: `TS/S12-homework.md`.

См. `HW12.ipynb`:

- использует датасет `TS/S12-hw-dataset.csv`;
- делает корректный **temporal split** 70/15/15 без перемешивания;
- сравнивает baseline-модели и GRU на оконном представлении;
- сохраняет артефакты в `./artifacts/` (таблица прогонов, конфиг, модель, графики).

Зависимости: `torch`, `pandas`, `numpy`, `scikit-learn`, `matplotlib`.
