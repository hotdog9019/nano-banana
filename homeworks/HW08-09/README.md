# HW08-09

Требования: `TS/S08-homework.md`.

См. `HW08-09.ipynb`:

- загружает датасет через `torchvision` в `../datasets/`;
- по умолчанию использует `CIFAR10` (переменная `DATASET` в ноутбуке);
- делает воспроизводимое разбиение train/val;
- запускает эксперименты E1–E4 и O1–O3;
- сохраняет артефакты в `./artifacts/` (таблица прогонов, конфиг, модель, графики).

Зависимости: `torch`, `torchvision`, `numpy`, `matplotlib`.
